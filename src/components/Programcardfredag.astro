
 
 
 <section class="">
    <article class="py-12">
        <section x-data="{ alleprogramfredag: [] }"
                 x-init="alleprogramfredag = await (await fetch('programfredag.json')).json(); alleprogramfredag.reverse();"
                 class="">
            <div class="flex justify-center items-center flex-col mb-12">
                <h2 class="font-khand text-[34px] font-bold text-rod-500  min-[950px]:text-[65px]">Fredag 7/6</h2>
                <h5 class="font-anek text-[16px] text-rod-500 mb-20 min-[950px]:text-[25px]">Der bliver løbende opdateret angående artister</h5>
            </div>

            <section class="flex justify-center items-center jsani1">
                <div class="w-full max-w-screen-xl mx-auto">
                    <section class="flex flex-wrap justify-center gap-12">
                        <template x-for="programfredag in alleprogramfredag" :key="programfredag.id" x-data="{ openbeskrivelselang: false }">
                            <div class="bg-sand-500 rounded-[18px] mb-9 w-[350px] ">
                                <article class="grid grid-cols-2">
                                    <img :src="programfredag.kunsterbilled" :alt="programfredag.kunsterbilledalt" class="-mt-[20%] -ml-[20%]" />
                                    <h4 x-text="programfredag.kunster" class="place-self-center text-rod-500 font-khand font-bold text-[30px]"></h4>
                                </article>
                                <p x-text="programfredag.beskrivelse" class="text-[16px] pt-6 px-8 font-anek">                                
                                    
</p>
<div x-show="openbeskrivelselang"><div x-text="programfredag.beskrivelselang" class="px-8 read-more-btn font-anek text-[16px]"></div></div>


<span class="px-8" x-on:click="openbeskrivelselang = !openbeskrivelselang">
    <button class="bg-blo-500 font-khand text-cream-500 close font-bold rounded-[7px] py-1 px-4 text-[13px] min-[950px]:text-[20px] min-[950px]:px-[30px]" x-text="openbeskrivelselang ? 'LÆS MINDRE' : 'LÆS MERE'"></button>
</span>

                                <div class="flex flex-row place-self-center px-8 py-12 justify-center text-rod-500 font-khand font-bold text-[30px]  space-x-6">
                                    <span>Spiller</span>
                                    <span x-text="programfredag.tidspunkt"></span>
                            </div>
                        </template>
                    </section>
                </div>
            </section>
        </section>
    </article>
</section>

<style>

    :root {
      --hover: #C7522A; /* Define the hover color */
    }
    
    .close:hover,
    .close:focus {
      box-shadow: 
        inset -4.5em 0 0 0 var(--hover),
        inset 4.5em 0 0 0 var(--hover);  
    }
    
    button {  
  
      transition: 0.25s;
      
      &:hover,
      &:focus { 
        border-color: var(--hover);
       
      }
    }
    
  
    </style>

<script>


    import { inView } from "motion";
    
    const jsani1 = document.querySelector(".jsani1");
    
    inView(jsani1, () => {
      jsani1.classList.add("animate__animated", "animate__fadeInDown");
    });
    
    
    
    
    </script>



